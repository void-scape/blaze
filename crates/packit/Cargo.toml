[package]
name = "packit"
version = "0.1.0"
edition = "2024"

[features]
preprocess = ["dep:image", "dep:rodio"]

[dependencies]
packit-macro = { path = "../packit-macro" }

image = { version = "0.25", optional = true }
rodio = { version = "0.21", optional = true }
